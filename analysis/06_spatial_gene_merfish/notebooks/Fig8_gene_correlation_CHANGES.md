# Figure 8 — spatial gene-expression correlation (MERFISH)

Two marimo notebooks (from your `Opioid_Figure 7_..._cleaned_temporary` files; note the old
"Figure 7" filename = manuscript **Figure 8**). The **searchlight** analysis (S16) is separate,
not included here.

## `Fig8_gene_correlation.py` — main figure
Per-region Pearson correlation of the semi-NMF spatial factors (Morphine / Withdrawal) with
voxel-level ABI-MERFISH gene expression (and cell-type cluster expression), permutation test +
BH; ECDF/close-up figures; opioid receptors (Oprm1/Oprd1/Oprk1) highlighted. Targets
`Acb`, `Ce`, `VTA`; factors `[0,1,2]`.

Changes (this notebook was already marimo):
- Repointed the config paths to `DATA_ROOT`: `GROUP = 06_spatial_gene_merfish`,
  `ATLAS_DIR = shared/atlas`, `FACTOR_DIR = GROUP/factors`, gene/cluster zarr + CSVs from
  `GROUP`, outputs -> `../figures/Figure8`. Removed the `\\10.159.50.7\...` and
  `OneDrive\...` paths.
- Fixed two `cleaned_temporary` leftovers: uncommented the `cluster_zarr = da.from_zarr(...)`
  load (it was commented out), and deleted two dead debug cells (a one-time cluster
  reorientation cell and a `gene_vol` `imshow` referencing an undefined variable).

## `Fig8_allen_preprocessing.py` — data generation (run first)
Downloads ABI MERFISH data, builds the Allen->Unified/Kim ANTs transform, voxelizes gene +
cluster expression into the Unified atlas, and **writes the inputs Fig 8 reads**:
`gene_expression.zarr`, `cluster_expression.zarr`, `gene_df.csv`, `cluster_details.csv`.
- Repointed: `ATLAS_DIR = shared/atlas`, `OUTPUT_DIR = 06_spatial_gene_merfish` (so outputs
  feed Fig 8); `PROJECT_DIR`/cache/transforms are a local working dir (`OPIOID_ALLEN_PROJECT`).

## Inputs / data (deposit `06_spatial_gene_merfish/`)
- **Generated by the preprocessing** (or provide precomputed): `gene_expression.zarr`,
  `cluster_expression.zarr`, `gene_df.csv`, `cluster_details.csv`.
- **PROVIDE:** `factors/factor{0,1,2}.npy` — semi-NMF spatial factors (from Figure 6).
- Preprocessing also needs, in `shared/atlas/`: `template_25_coronal.tif`,
  `Kim_ref_adult_v1_brain.tif` (in addition to the Kim FP-label v4.0 already there), plus the
  Allen ABC-atlas cache (auto-downloaded to the working dir) and `ants`.

## Requires
`dask`, `zarr`, `tifffile`, `statsmodels`, `ants` (preprocessing), `abc-atlas-access`
(preprocessing) — all in requirements.txt.
